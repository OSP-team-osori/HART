apiVersion: 1

datasources:
  - name: PostgreSQL
    uid: hart-postgres
    type: postgres
    url: postgres:5432
    user: postgres
    secureJsonData:
      password: password
    jsonData:
      database: agent_harness
      sslmode: disable
      postgresVersion: 1600
      timescaledb: false
    isDefault: true
